# RA Native Replacement Memo v1

This pass treats *every* non-native or hybrid result as requiring one of four outcomes:

1. **rename_native** — the proof content is salvageable and can stay after a vocabulary replacement.
2. **split_and_replace** — the current declaration mixes native and imported claims; split it into native subtheorems.
3. **reframe_nature_target** — the theorem targets a theory object (`alpha_s`, hypercharge, GMN, etc.) and must be replaced by a direct Nature-facing research task.
4. **archive_bridge / retire_non_native** — the artifact is not on the active native path.

## The sharpest replacements

### 1. D1 core
Keep the arithmetic / motif dynamics, but rename the whole file into motif/depth/orientation language. Remove Section 15 and Section 16 from the active root.

### 2. O14
Keep only the arithmetic BDG-coefficient core. Remove alpha/coupling overlays from the active root.

### 3. Baryon/Chirality file
Replace the entire file by a native orientation-and-ledger module.

### 4. AQFT / GR / coupling files
Archive them. They are not current proof targets under the April 20 framing discipline.

## Immediate implementation order

1. land the strict native root
2. land `RA_BDG_Coefficient_Arithmetic`
3. land `RA_MotifDynamics_Core`
4. land `RA_CausalOrientation_Core`
5. rewrite Paper I and Paper II so the prose matches the new root
