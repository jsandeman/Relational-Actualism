# RA Native Research Replacement Docket v1

This docket converts the remaining non-native and hybrid results into direct Nature-facing native tasks.

## A. Retire from the active root now

| legacy target | why it is retired | replacement path |
|---|---|---|
| AQFT / CFC / Unruh chain | continuum adapter, not ontology-bearing | archival bridge appendix only |
| `alpha_*` overlay theorems | theory-object targets | direct prediction of measured interaction strengths |
| Koide / hypercharge / isospin declarations | imported categories | internal family/orientation rules tied directly to observed masses and charges |
| field-equation uniqueness | bridge target | direct macroscopic observables programme |
| KCB / Landauer / Maxwell / spin-bath | hybrid quantum-information overlay | defer until a fully native irreversible-computation programme exists |

## B. Replace by direct Nature-facing tasks

| current label | native replacement task | direct measurable target |
|---|---|---|
| WIMP prohibition | predict continued null direct-detection results for non-actualizing hidden sectors | detector nulls below neutrino floor |
| `alpha_EM` overlay | derive the measured fine-structure constant directly from DAG + BDG + LLC | atomic spectra / measured `1/137.036...` |
| `alpha_s` overlay | predict hadronic binding scale and observed lifetime/mass patterns without QCD labels | measured hadron masses and widths |
| Koide relation | predict charged-lepton mass pattern directly | `m_e`, `m_μ`, `m_τ` |
| weak-field source law | derive orbital/lensing response from sparse-regime graph variables | Mercury, light deflection, lensing maps |
| MOND / WIMP / Bullet wording | replace with direct galaxy and cluster observables | rotation curves, lensing offsets |
| DFT/F1 support | compute closure support from observed reaction networks and persistence data | prebiotic/biochemical network stability windows |
| Assembly Theory convergence | compare only after the RA-native quantity is computed | empirical correlation study, not proof target |
| KCB | derive a fully native irreversible-array scaling ceiling, if it exists | observed scaling breakdown in large arrays |
| Landauer / Maxwell | derive actualization cost per irreversible memory update from native graph bookkeeping | bit-erasure and measurement energetics |

## C. Immediate theorem tasks that remain genuinely native

1. strengthen `ledger_flux_zero_across_cut` as the canonical severance bookkeeping theorem
2. canonize `CausalShield` and remove `MarkovBlanket` from the active public vocabulary
3. finish the in-repo split of D1 into core / translation / bridge
4. isolate O14 arithmetic as a root module
5. replace `RA_BaryonChirality` by `RA_CausalOrientation_Core`
6. build direct Nature-facing scripts from measured reaction / lifetime / orbital data rather than theory objects

## D. Native question filter

A theorem or script stays active only if all answers are yes:

1. does it use only DAG + BDG + LLC + direct measurements?
2. is the target a measured thing from Nature?
3. are all intermediate categories internal to RA?
4. can the result be stated without QFT/SM/GR vocabulary?

If any answer is no, the item is not active-root material.
