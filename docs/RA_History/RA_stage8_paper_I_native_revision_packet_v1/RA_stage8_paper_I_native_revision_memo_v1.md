# Paper I native revision memo v1

This pass aligns Paper I with the same native-first discipline now governing Papers II--IV.

## Main changes

- Abstract rewritten so the active kernel is stated directly in DAG + BDG + LLC terms.
- Epistemic-status and suite-placement boxes rebuilt around the active native support surface.
- Introduction rewritten so Nature is the target and historical continuum theories are not treated as intermediate milestones.
- Axiom of stochastic actualization rewritten to avoid making continuum wave-equation language primitive.
- The old four-level actualization hierarchy is replaced by a kernel-first account:
  - active criterion: `S_BDG > 0`
  - derived statistical readout: `P_acc(μ)` and the selectivity witness `ΔS*`
  - entropic / AQFT / on-shell language moved to deferred cartography.
- The Unruh / Rindler section is removed from the active derivational chain and replaced by a short deferred-bridge note.
- The measurement section is rewritten in native sparse-versus-dense regime language; Schrödinger/Born/AQFT wording is downgraded to deferred cartography.
- The old relativistic section is narrowed to finite propagation, event-count time, and saturation / partition structure.
- The `d=4` section is rewritten to separate active arithmetic support from downstream geometric cartography.
- The coupling-constant section is replaced by a short archival note; `α_s`, `α_EM`, and Koide overlays are no longer active support.
- Antichain drift is retained only as a local graph statistic, not as a closed cosmological mechanism.
- The Lean table is rebuilt around the current compiled native support surface only:
  - `RA_GraphCore.lean`
  - `RA_O01_KernelLocality_v2.lean`
  - `RA_O14_ArithmeticCore_v1.lean`
- Predictions are replaced by a suite-level downstream-target note.
- The epistemic summary now uses `ANC / DR / CV / AR / Deferred / Archived`.

## Status honesty

This is a source-level TeX revision. It has not been PDF-compiled in this environment.
