--- RA_Paper_I_Kernel_and_Engine_patched_AQFT_v1.tex
+++ RA_Paper_I_Kernel_and_Engine_patched_AQFT_v2.tex
@@ -646,7 +646,7 @@
 \end{tabular}
 \end{center}
 
-\noindent$^\dagger$\textbf{Conditional Lean results in \texttt{RA\_AQFT\_v10.lean}:} the frame-independence and Rindler-stationarity theorems are proved in Lean modulo (a)~one live CFC lemma---the general commutation statement \texttt{Matrix.cfc\_conj\_unitary}---and (b)~two imported axioms---\texttt{vacuum\_lorentz\_invariant} (Poincar\'e invariance of the Minkowski vacuum, from the Wightman axioms) and \texttt{petz\_monotonicity} (data processing inequality, for the CPTP branch). Only \texttt{vacuum\_lorentz\_invariant} is load-bearing for frame independence and Rindler stationarity; \texttt{petz\_monotonicity} is not invoked by these theorems. The attempted Lean-QuantumInfo-to-Mathlib port of the CFC proof chain failed against the current Mathlib API and was reverted, so the remaining CFC step should be described as a narrow current-Mathlib closure target rather than as a copied proof already in hand. Replacing \texttt{vacuum\_lorentz\_invariant} with an RA-native derivation from the BDG sprinkling limit is a separate open target.
+\noindent$^\dagger$\textbf{Conditional Lean results in \texttt{RA\_AQFT\_v10.lean}:} the frame-independence and Rindler-stationarity theorems are proved in Lean modulo (a)~one live CFC lemma---the general commutation statement \texttt{Matrix.cfc\_conj\_unitary}---and (b)~two imported axioms---\texttt{vacuum\_lorentz\_invariant} (Poincar\'e invariance of the Minkowski vacuum, from the Wightman axioms) and \texttt{petz\_monotonicity} (data processing inequality, for the CPTP branch). Only \texttt{vacuum\_lorentz\_invariant} is load-bearing for frame independence and Rindler stationarity; \texttt{petz\_monotonicity} is not invoked by these theorems. The attempted Lean-QuantumInfo-to-Mathlib port of the CFC proof chain failed against the current Mathlib API and was reverted, so the remaining CFC step should be described as a narrow current-Mathlib closure target rather than as a copied proof already in hand; the repository now carries a Mathlib-only support port candidate, but canonical status should still wait on a clean build. Replacing \texttt{vacuum\_lorentz\_invariant} with an RA-native derivation from the BDG sprinkling limit is a separate open target.
 
 Total: 176+ theorems across 8 files, $\sim$3000 lines of Lean code. The proof files are publicly available at \texttt{github.com/jsandeman/Relational-Actualism}.
 
@@ -677,7 +677,7 @@
 %% ═══════════════════════════════════════════════════════════
 
 \begin{tcolorbox}[title=Scope of the Lean-verified layer,colback=gray!5,colframe=gray!60]
-The Lean-verified core of this paper consists of: the kernel identities on the discrete DAG (LLC, graph-cut, Markov blanket, amplitude locality in the BDG increment model), the integer-arithmetic identities (BDG coefficient inversion at the arithmetic layer, $\alpha_{\mathrm{EM}}^{-1}=137$, Koide $K=2/3$, confinement depths, BDG closure), and several finite-dimensional AQFT-layer theorems conditional on disclosed inputs. Specifically, \texttt{frame\_independence} and \texttt{rindler\_stationarity} in \texttt{RA\_AQFT\_Proofs\_v10.lean} depend on one live CFC lemma (\texttt{Matrix.cfc\_conj\_unitary}) and one QFT-imported axiom (\texttt{vacuum\_lorentz\_invariant}, stating Poincar\'e invariance of the Minkowski vacuum). The remaining CFC step is a narrow library-closure target; the broader open frontier is the local-algebra AQFT layer needed to define \(P_{\mathrm{act}}\) rigorously and prove modular-flow commutativity. Uniqueness of the BDG coefficients is Lean-verified at the arithmetic layer and supplemented by the peer-reviewed geometric identification of Yeats~(2025). Papers II--IV inherit this kernel and should treat claims depending on AQFT-layer closure or fully-native BDG-geometric uniqueness as conditional on the corresponding gaps in this paper.
+The Lean-verified core of this paper consists of: the kernel identities on the discrete DAG (LLC, graph-cut, Markov blanket, amplitude locality in the BDG increment model), the integer-arithmetic identities (BDG coefficient inversion at the arithmetic layer, $\alpha_{\mathrm{EM}}^{-1}=137$, Koide $K=2/3$, confinement depths, BDG closure), and several finite-dimensional AQFT-layer theorems conditional on disclosed inputs. Specifically, \texttt{frame\_independence} and \texttt{rindler\_stationarity} in \texttt{RA\_AQFT\_Proofs\_v10.lean} depend on one CFC lemma (\texttt{Matrix.cfc\_conj\_unitary}) and one QFT-imported axiom (\texttt{vacuum\_lorentz\_invariant}, stating Poincar\'e invariance of the Minkowski vacuum). A Mathlib-only support port for the CFC step now lives in \texttt{RA\_CFC\_Port.lean}, but the integrated build still needs verification on the pinned toolchain before the CFC layer is described as fully discharged. The broader open frontier is the local-algebra AQFT layer needed to define \(P_{\mathrm{act}}\) rigorously and prove modular-flow commutativity. Uniqueness of the BDG coefficients is Lean-verified at the arithmetic layer and supplemented by the peer-reviewed geometric identification of Yeats~(2025). Papers II--IV inherit this kernel and should treat claims depending on AQFT-layer closure or fully-native BDG-geometric uniqueness as conditional on the corresponding gaps in this paper.
 \end{tcolorbox}
 
 \begin{center}
@@ -726,7 +726,7 @@
 
 \textbf{Legend:} LV = Lean-verified theorem (machine-checked). LV (conditional) = Lean-verified modulo a disclosed axiom or sorry, with the dependency stated in the Lean-verification table. DR = derived (all steps explicit within RA). CV = computation-verified (explicit numerical evaluation). AR = argued (physically motivated, not all steps explicit). Open = conjectured with a stated path to closure.
 
-\noindent$^\dagger$\textbf{Conditional on:} the sorry \texttt{Matrix.cfc\_conj\_unitary} (trivially closable from Lean-QuantumInfo) and the QFT-imported axiom \texttt{vacuum\_lorentz\_invariant} in \texttt{RA\_AQFT\_Proofs\_v10.lean}. See the Lean-verification table and \S\ref{sec:unruh} for details.
+\noindent$^\dagger$\textbf{Conditional on:} the CFC-unitary commutation lemma \texttt{Matrix.cfc\_conj\_unitary} and the QFT-imported axiom \texttt{vacuum\_lorentz\_invariant} in \texttt{RA\_AQFT\_Proofs\_v10.lean}. The repository now carries a Mathlib-only support port (\texttt{RA\_CFC\_Port.lean}) and the direct integration patch is straightforward, but compile verification on the pinned toolchain remains pending in this audit environment. See the Lean-verification table and \S\ref{sec:unruh} for details.
 
 %% ═══════════════════════════════════════════════════════════
 \section{Conclusion}
