# RA Graph Orientation Closure Programme — Apr 29 2026

## Purpose

`RA_GraphOrientationClosure_v1.lean` is the next safe rung in the Selector Closure / topological ledger programme.

Previous compiled files established:

```text
FrontierOrientationChart supplied
  -> IncidenceSignSource
  -> signed three-direction N1 frame
  -> seven-value charge ledger
```

and then:

```text
GraphOrientationData supplied
  -> FrontierOrientationChart
  -> deterministic incidence signs
```

This file packages the existing compiled RA orientation theorem surface:

```text
RA_CausalOrientation_Core.lean
RA_D1_NativeLedgerOrientation_v1.lean
```

as native evidence for a future graph-orientation closure theorem.

## Main conceptual point

The charge-sign rule is no longer being treated as a random sampled label. The formal ladder now says:

```text
compiled orientation evidence + frontier chart-determination principle
  -> graph-orientation closure certificate
  -> deterministic sign source
  -> seven-value signed N1 ledger
```

The supplied element is still the actual orientation data / chart-determination principle. That remains the hard theorem.

## What remains open

The next hard target is:

```text
finite Hasse frontier
  + causal-orientation asymmetry
  + D1 ledger-orientation preservation
  + no actual-history quotient
  + no random labels
  -> GraphOrientationClosureCertificate
```

This would make the sign-source graph-native rather than chart-supplied.
