# Track B.3: v1.8.1-Compliant Witness Coverage Sweep

Track B.2 validated that the active graph/cut witness extractor can produce member-index-free orientation witness rows on active simulator graph states. It did not establish that those rows satisfy the v1.8.1 comparison-domain requirements.

This packet makes v1.8.1 the controlling standard. It asks whether fixed orientation-overlap bins populate low/high contrasts inside support_width × family_size strata. If not, orientation-specific rescue remains non-estimable.

This packet deliberately avoids rescue analysis. It produces coverage diagnostics and sampler recommendations only.
