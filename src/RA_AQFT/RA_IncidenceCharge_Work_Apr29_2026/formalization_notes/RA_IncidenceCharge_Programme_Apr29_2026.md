# RA Incidence Charge v1 — Programme Notes

This theorem step begins the topological ledger / charge-sign layer.

The compiled Hasse-frontier ladder now supplies graph-native candidate pasts and finite graph-derived frontiers.  `RA_IncidenceCharge_v1.lean` adds the next conditional object: an incidence-sign source on oriented frontier links.

The file deliberately does not claim that the sign source has been derived.  It proves the safe conditional theorem:

```text
three incidence signs in {-1,0,+1}
  → net signed N1 charge in {-3,-2,-1,0,+1,+2,+3}
```

This is the formal analogue of the source-backed RA charge signature.  The next hard theorem is to construct `IncidenceSignSource` from finite oriented boundary/incidence data rather than supplying it.

Conceptual status:

```text
signed N1 seven-value range: formal conditional theorem
edge-level sign source: still open
ledger/topology bridge: started, not closed
```
