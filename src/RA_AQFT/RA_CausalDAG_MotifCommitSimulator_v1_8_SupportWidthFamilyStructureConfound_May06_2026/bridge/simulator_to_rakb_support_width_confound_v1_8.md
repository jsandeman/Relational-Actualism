# Simulator-to-RAKB Bridge: v1.8 Support-Width / Family-Structure Confound Audit

v1.8 should be registered, if confirmed canonically, as a corrective analysis layer over v1.7. It should not be framed as an orientation-specific result.

Candidate claim posture:

```text
v1.8 tests whether v1.7 orientation-overlap gaps are explained by support-width and family-structure binning rather than orientation-link specificity.
```

Recommended caveat:

```text
Claims should be promoted only after running against canonical v1.7 outputs. Packet-local outputs are subset diagnostics.
```
