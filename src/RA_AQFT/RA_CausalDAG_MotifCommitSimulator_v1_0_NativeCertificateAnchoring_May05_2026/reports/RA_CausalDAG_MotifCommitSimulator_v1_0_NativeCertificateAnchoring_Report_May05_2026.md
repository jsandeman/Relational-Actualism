# RA v1.0 Native Certificate Anchoring Report

This packet is a consolidation step after v0.9.2. It formalizes the component vocabulary for native certificate overlap and packages the simulator-side analysis that reports component profiles, primary/guardrail family semantics, and qualitative calibration status.

## Status

- Lean bridge: source-level pending local compile.
- Python analysis: unit-tested in this environment.
- Simulator semantics: unchanged from v0.9/v0.9.2 lineage.
- RAKB proposals: active-schema style, manual application recommended.

## Research posture

v1.0 does not claim a derived BDG–LLC probability law. It prepares the formal and analytical surface for one.

The current result should be read as:

> Native certificate-witness overlap is represented as a component profile tied to RA-native support/frontier/orientation/ledger/causal-past/kernel/firewall surfaces; certification-rescue signatures remain qualitative until the component weights and overlap law are derived or more tightly constrained by the foundational stack.
