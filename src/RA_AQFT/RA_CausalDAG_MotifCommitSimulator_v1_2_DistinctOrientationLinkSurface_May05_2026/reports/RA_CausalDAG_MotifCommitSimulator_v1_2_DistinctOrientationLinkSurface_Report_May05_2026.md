# RA v1.2 Distinct Orientation-Link Surface Report

This packet implements the next step after v1.1: create and audit a distinct
orientation-link/sign-source surface rather than reusing the support/frontier
proxy as orientation overlap.

The packet is designed to be conservative:

- it exposes the old triplet confounding;
- it adds a separate orientation-link overlap proxy;
- it tests matched support/frontier strata for orientation variation;
- it keeps selector stress outside support/certification-family rescue;
- it marks the result as a prototype pending native-orientation derivation.

Canonical results should be produced from the installed v1.0 output directory.
