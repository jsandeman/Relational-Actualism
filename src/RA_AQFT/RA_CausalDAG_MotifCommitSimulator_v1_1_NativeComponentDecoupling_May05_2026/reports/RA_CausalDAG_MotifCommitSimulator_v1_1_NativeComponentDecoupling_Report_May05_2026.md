# RA v1.1 Native Component Decoupling Report

This packet audits whether the v1.0 component surfaces are sufficiently separated to support
component-specific attribution claims.

The intended scientific use is conservative:

1. Preserve the v1.0 low-overlap/high-rescue result.
2. Preserve clean ledger attribution where ledger overlap is separately resolved.
3. Avoid overclaiming orientation-specificity when orientation overlap is numerically tied to
   support/frontier overlap.

The packet-local run uses the v1.0 outputs available in this workspace. Canonical interpretation
should use the committed v1.0 outputs.
