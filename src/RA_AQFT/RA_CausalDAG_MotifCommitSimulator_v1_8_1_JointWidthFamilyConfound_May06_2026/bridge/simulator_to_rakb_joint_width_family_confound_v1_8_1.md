# v1.8.1 Simulator-to-RAKB Bridge

This packet provides analysis evidence for a pending confound-resolution node:

```text
orientation-overlap rescue gap
  → raw estimate
  → width-matched estimate
  → width × family-size matched estimate
```

The RAKB claims should remain pending canonical analysis until the user's v1.7 canonical outputs are processed.

Recommended framing:

```text
v1.8.1 is a joint-stratification audit. It should not be used to promote a
residual orientation-specificity claim unless the graph-derived gap survives
support_width × family_size matching and differs from shuffled controls.
```
