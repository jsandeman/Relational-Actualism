# Bridge Note: Track A and the v1.x Audit Cycle

Track A uses the v1.9 synthesis as its epistemic boundary.

Included:

```text
support-family route/readiness rescue
independent certified-family readiness
strict-parent rescue
family-internal resilience
```

Excluded:

```text
orientation-specific certification rescue
orientation-overlap keying
matched-graph orientation claims
```

Reason: v1.8.1 is the controlling audit for orientation-specificity and currently supports no independent matched-graph orientation-specific rescue signal.
