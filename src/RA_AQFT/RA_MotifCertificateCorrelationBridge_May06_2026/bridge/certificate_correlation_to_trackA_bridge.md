# Bridge to Track A and v0.8/v0.8.1

`RA_MotifCertificationResilienceConsolidation` formalized the metric-repair distinction:

- strict-parent rescue;
- family-internal resilience.

This packet adds qualitative certificate-fate vocabulary:

- member-distinct fates;
- shared-fate families;
- weakly shared fates;
- an abstract sharing preorder.

The goal is to prepare the next native-overlap bridge without asserting numerical simulation claims in Lean. The external simulator correlation parameter from v0.8/v0.8.1 should be read as an operational sampling device; the Lean bridge records only the structural surfaces that such a parameter ranges over.
