# RA Track B.4 Locked-Envelope Matched-Graph Rescue Analysis

Track B.3b found a narrow v1.8.1-valid coverage envelope. B.4 asks whether that envelope supports a graph-derived incidence-role-signed rescue association after fixed-bin joint support-width × family-size controls and a shuffled-overlap control.

This is a locked-envelope diagnostic, not a general orientation-rescue analysis.

The packet follows the v1.9 and v1.8.1 guardrails:

1. no member-indexed keying is counted;
2. shuffled controls are controls, not valid native-witness keyings;
3. bins remain fixed across confound controls;
4. joint support-width × family-size strata are required;
5. if strata are not estimable, the result is non-estimability;
6. no orientation-rescue claim is made by the packet itself.
